<Window
    x:Class="Aura.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Aura.App"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <!-- Navigation panel -->
        <NavigationView 
            x:Name="NavView"
            Grid.Row="0"
            IsSettingsVisible="True"
            IsBackButtonVisible="Collapsed"
            PaneDisplayMode="Left"
            SelectionChanged="NavView_SelectionChanged"
            PaneTitle="Aura Video Studio"
            IsPaneToggleButtonVisible="True">
            
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Create" Tag="create" Icon="Add" ToolTipService.ToolTip="Create a new video project"/>
                <NavigationViewItem Content="Storyboard" Tag="storyboard" Icon="Edit" ToolTipService.ToolTip="Edit video timeline and scenes"/>
                <NavigationViewItem Content="Render" Tag="render" Icon="Video" ToolTipService.ToolTip="Export your video"/>
                <NavigationViewItem Content="Library" Tag="library" Icon="Library" ToolTipService.ToolTip="View past projects"/>
                <NavigationViewItemSeparator />
                <NavigationViewItem Content="Hardware Profile" Tag="hardware" Icon="DeviceLaptop" ToolTipService.ToolTip="System information and capabilities"/>
            </NavigationView.MenuItems>
            
            <Frame x:Name="ContentFrame" />
        </NavigationView>
        
        <!-- Status bar -->
        <Grid 
            Grid.Row="1"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="0,1,0,0"
            Height="32">
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <StackPanel 
                Grid.Column="0"
                Orientation="Horizontal"
                Spacing="4"
                Margin="16,0">
                <FontIcon 
                    Glyph="&#xE9D9;" 
                    FontSize="12"
                    Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                <TextBlock 
                    x:Name="StatusText"
                    Text="Ready" 
                    VerticalAlignment="Center"
                    FontSize="12"/>
            </StackPanel>
            
            <StackPanel 
                Grid.Column="2"
                Orientation="Horizontal"
                Spacing="20"
                Margin="16,0">
                
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon 
                        Glyph="&#xEC4F;" 
                        FontSize="12"
                        Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                    <TextBlock 
                        x:Name="EncoderInfoText"
                        Text="Encoder: Auto"
                        VerticalAlignment="Center"
                        FontSize="12"/>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon 
                        Glyph="&#xE946;" 
                        FontSize="12"
                        Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                    <TextBlock 
                        x:Name="CacheInfoText"
                        Text="Cache: 0 MB"
                        VerticalAlignment="Center"
                        FontSize="12"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Window>