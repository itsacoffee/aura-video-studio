# Model Selection Architecture - Visual Guide

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌────────────────┐  ┌──────────────────┐  ┌────────────────┐ │
│  │ ModelsPage     │  │ ModelPicker      │  │ Job Details    │ │
│  │                │  │                  │  │                │ │
│  │ • Audit Log    │  │ • Badges/Tooltips│  │ • Audit Trail  │ │
│  │ • Fallback ℹ️   │  │ • Test Model 🧪  │  │ • Per-Job View │ │
│  └────────────────┘  └──────────────────┘  └────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                          API LAYER                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  GET  /api/models/selection              → Get all selections  │
│  POST /api/models/selection              → Set selection       │
│  GET  /api/models/audit-log              → Global audit        │
│  GET  /api/models/audit-log/job/{jobId}  → Job-specific audit │
│  POST /api/models/test                   → Test model          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                       BUSINESS LOGIC                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ModelSelectionService                                          │
│  ├─ ResolveModelAsync() ──────────────────┐                   │
│  │  • Applies precedence rules             │                   │
│  │  • Tracks fallback reasons              │                   │
│  │  • Records audit trail                  │                   │
│  │                                         ▼                   │
│  ├─ SetModelSelectionAsync()    ┌──────────────────┐         │
│  │  • Validates model exists      │ Precedence Logic│         │
│  │  • Checks deprecation          │                 │         │
│  │  • Stores selection            │ 1. Pinned Run   │         │
│  │                                │ 2. Run Override │         │
│  └─ GetAuditLogByJobIdAsync()    │ 3. Pinned Stage │         │
│     • Retrieves job audit         │ 4. Project      │         │
│                                   │ 5. Global       │         │
│                                   │ 6. Auto-Fallback│         │
│                                   └──────────────────┘         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      DATA PERSISTENCE                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ModelSelectionStore                                            │
│  ├─ model-selections.json                                      │
│  │  {                                                           │
│  │    "selections": [                                           │
│  │      {                                                       │
│  │        "provider": "OpenAI",                                 │
│  │        "stage": "script",                                    │
│  │        "modelId": "gpt-4",                                   │
│  │        "scope": "Stage",                                     │
│  │        "isPinned": true                                      │
│  │      }                                                       │
│  │    ],                                                        │
│  │    "auditLog": [                                             │
│  │      {                                                       │
│  │        "provider": "OpenAI",                                 │
│  │        "stage": "script",                                    │
│  │        "modelId": "gpt-4o-mini",                             │
│  │        "source": "AutomaticFallback",                        │
│  │        "fallbackReason": "gpt-4 was unavailable",           │
│  │        "jobId": "job-123",                                   │
│  │        "timestamp": "2025-11-06T00:00:00Z"                  │
│  │      }                                                       │
│  │    ],                                                        │
│  │    "allowAutomaticFallback": false                          │
│  │  }                                                           │
│  └─────────────────────────────────────────────────────────────│
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## Resolution Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    Model Resolution Flow                         │
└─────────────────────────────────────────────────────────────────┘

    ┌──────────────┐
    │ Request      │
    │ with stage   │
    └──────┬───────┘
           │
           ▼
    ┌─────────────────────────┐
    │ Check Run Override      │◄─── CLI/API parameter
    │ (Pinned?)               │
    └──────┬──────────────────┘
           │
      ┌────┴────┐
      │Available?│
      └────┬────┘
           │
    ┌─────▼──────┐
    │   YES      │                  ┌──────────────┐
    │  ┌─────┐   │                  │   NO         │
    │  │ USE │   │                  │  ┌────────┐  │
    │  └─────┘   │                  │  │ BLOCK  │  │ ◄── Pinned
    └────────────┘                  │  └────────┘  │
                                    └──────────────┘
           │
           ▼
    ┌─────────────────────────┐
    │ Check Stage Pinned      │◄─── User configured
    └──────┬──────────────────┘
           │
      ┌────┴────┐
      │Available?│
      └────┬────┘
           │
    ┌─────▼──────┐
    │   YES      │                  ┌──────────────┐
    │  ┌─────┐   │                  │   NO         │
    │  │ USE │   │                  │  ┌────────┐  │
    │  └─────┘   │                  │  │ BLOCK  │  │ ◄── Pinned
    └────────────┘                  │  └────────┘  │
                                    └──────────────┘
           │
           ▼
    ┌─────────────────────────┐
    │ Check Project Override  │◄─── Per-project setting
    └──────┬──────────────────┘
           │
      ┌────┴────┐
      │Available?│
      └────┬────┘
           │
    ┌─────▼──────┐              ┌──────────────────┐
    │   YES      │              │   NO             │
    │  ┌─────┐   │              │  Record Fallback │
    │  │ USE │   │              │  Continue ▼      │
    │  └─────┘   │              └──────────────────┘
    └────────────┘
           │
           ▼
    ┌─────────────────────────┐
    │ Check Global Default    │◄─── Application-wide
    └──────┬──────────────────┘
           │
      ┌────┴────┐
      │Available?│
      └────┬────┘
           │
    ┌─────▼──────┐              ┌──────────────────┐
    │   YES      │              │   NO             │
    │  ┌─────┐   │              │  Record Fallback │
    │  │ USE │   │              │  Continue ▼      │
    │  └─────┘   │              └──────────────────┘
    └────────────┘
           │
           ▼
    ┌─────────────────────────┐
    │ Auto-Fallback Enabled?  │◄─── User toggle
    └──────┬──────────────────┘
           │
      ┌────┴────┐
      │ Enabled?│
      └────┬────┘
           │
    ┌─────▼──────┐              ┌──────────────────┐
    │   YES      │              │   NO             │
    │  Use Safe  │              │  ┌────────────┐  │
    │  Default   │              │  │ BLOCK with │  │
    │  ┌─────┐   │              │  │ Recommend  │  │
    │  │ USE │   │              │  └────────────┘  │
    │  └─────┘   │              └──────────────────┘
    └────────────┘
           │
           ▼
    ┌─────────────────────────┐
    │  Record in Audit Log    │
    │  • Source               │
    │  • Reasoning            │
    │  • Fallback Reason      │
    │  • Timestamp            │
    │  • Job ID               │
    └─────────────────────────┘
```

## UI Components Structure

```
┌───────────────────────────────────────────────────────────────┐
│                     ModelsPage.tsx                            │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ Per-Stage Model Configuration                           │ │
│  ├─────────────────────────────────────────────────────────┤ │
│  │                                                         │ │
│  │  Script Generation                                      │ │
│  │  ┌────────────────┐ ┌───┐ ┌──────┐                    │ │
│  │  │ gpt-4          ▼│ │📌 │ │🧪Test│                    │ │
│  │  └────────────────┘ └───┘ └──────┘                    │ │
│  │  🔴 Pinned  🔵 Stage Override                          │ │
│  │                                                         │ │
│  │  Visual Prompts                                         │ │
│  │  ┌────────────────┐ ┌───┐ ┌──────┐                    │ │
│  │  │ gpt-4o         ▼│ │   │ │🧪Test│                    │ │
│  │  └────────────────┘ └───┘ └──────┘                    │ │
│  │  🔵 Project Override                                    │ │
│  │                                                         │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                               │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ Settings                                                │ │
│  ├─────────────────────────────────────────────────────────┤ │
│  │                                                         │ │
│  │  Allow automatic fallback  [ON/OFF]                    │ │
│  │                                                         │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                               │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ Audit Log (Recent)                                      │ │
│  ├─────────────────────────────────────────────────────────┤ │
│  │                                                         │ │
│  │  OpenAI / script → gpt-4o-mini                         │ │
│  │  Source: AutomaticFallback (2025-11-06 12:00:00)      │ │
│  │  Reasoning: Using automatic fallback                   │ │
│  │  ℹ️ Fallback: Project-override 'gpt-4' unavailable     │ │
│  │  Job ID: job-abc123                                    │ │
│  │                                                         │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                               │
└───────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────┐
│                  RunDetailsPage.tsx                           │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│  [Telemetry Data...]                                         │
│                                                               │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ Model Selection Audit Trail                             │ │
│  ├─────────────────────────────────────────────────────────┤ │
│  │                                                         │ │
│  │  OpenAI / script                                        │ │
│  │  Model: gpt-4                                           │ │
│  │  🟡 Stage Pinned  🔒 Pinned  ✓ Used                    │ │
│  │                                                         │ │
│  │  Reasoning: Using stage-pinned model: gpt-4            │ │
│  │  Selected at: 2025-11-06 12:00:00                      │ │
│  │                                                         │ │
│  │  ────────────────────────────────────────────────      │ │
│  │                                                         │ │
│  │  OpenAI / visual                                        │ │
│  │  Model: gpt-4o-mini                                     │ │
│  │  🟢 Automatic Fallback  ✓ Used                         │ │
│  │                                                         │ │
│  │  Reasoning: Using automatic fallback: Safe default     │ │
│  │  ℹ️ Fallback: Project-override 'gpt-4o' unavailable    │ │
│  │  Selected at: 2025-11-06 12:00:05                      │ │
│  │                                                         │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                               │
│  ℹ️ Selection Precedence: Run Override (Pinned) > Run       │
│     Override > Stage Pinned > Project Override > Global     │
│     Default > Automatic Fallback                            │
│                                                               │
└───────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────┐
│            Test Model Dialog (ModelPicker)                    │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│  Test Model: gpt-4                                           │
│  ─────────────────────────────────────────────────────────   │
│                                                               │
│  Test if the model gpt-4 from provider OpenAI is            │
│  available and working properly.                             │
│                                                               │
│  API Key (required) *                                        │
│  ┌────────────────────────────────────────────┐             │
│  │ ••••••••••••••••••••••••••••••             │             │
│  └────────────────────────────────────────────┘             │
│                                                               │
│  ℹ️ Note: Your API key is not stored and only used for      │
│     this test.                                                │
│                                                               │
│  ✓ Model is available and working!                          │
│    Context: 128,000 tokens                                   │
│                                                               │
│  ┌───────┐  ┌─────────────┐                                 │
│  │ Close │  │ 🧪 Test Model│                                 │
│  └───────┘  └─────────────┘                                 │
│                                                               │
└───────────────────────────────────────────────────────────────┘
```

## Badge Legend

```
Badge Color & Icon Key:

🔴 Run Override (Pinned)     → Highest precedence, blocks if unavailable
🟠 Run Override              → High precedence, falls back if unavailable
🟡 Stage Pinned              → Per-stage lock, blocks if unavailable
🔵 Project Override          → Project-level preference
⚪ Global Default            → Application-wide default
🟢 Automatic Fallback        → Safe catalog default (if enabled)

🔒 Pinned                    → Will never auto-change
⚠️ Deprecated                → Consider migrating
✓ Used                       → Successfully used in job
⚠️ Blocked                   → Unavailable and requires user action
```

## Data Flow Example

```
User Initiates Job
       │
       ▼
┌──────────────────┐
│ Stage: script    │
│ Provider: OpenAI │
└────────┬─────────┘
         │
         ▼
┌─────────────────────────────────┐
│ ResolveModelAsync()             │
│ • No run override               │
│ • Stage pinned: gpt-4           │
│ • Check availability...         │
└────────┬────────────────────────┘
         │
         ▼ (gpt-4 unavailable)
┌─────────────────────────────────┐
│ Pinned model unavailable!       │
│ • Block execution               │
│ • Return alternatives           │
│ • Record audit entry            │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│ Audit Entry                     │
│ {                               │
│   "modelId": "",                │
│   "source": "StagePinned",      │
│   "isBlocked": true,            │
│   "blockReason": "Stage-pinned  │
│     model 'gpt-4' unavailable", │
│   "alternatives": [             │
│     "gpt-4o", "gpt-4o-mini"     │
│   ]                             │
│ }                               │
└─────────────────────────────────┘
```

## API Request/Response Examples

### Test Model
```http
POST /api/models/test
Content-Type: application/json

{
  "provider": "OpenAI",
  "modelId": "gpt-4",
  "apiKey": "sk-..."
}

Response 200 OK:
{
  "provider": "OpenAI",
  "modelId": "gpt-4",
  "isAvailable": true,
  "isDeprecated": false,
  "contextWindow": 8192,
  "maxTokens": 8192,
  "testedAt": "2025-11-06T00:00:00Z"
}
```

### Get Job Audit
```http
GET /api/models/audit-log/job/job-abc123

Response 200 OK:
{
  "jobId": "job-abc123",
  "entries": [
    {
      "provider": "OpenAI",
      "stage": "script",
      "modelId": "gpt-4",
      "source": "StagePinned",
      "reasoning": "Using stage-pinned model: gpt-4",
      "isPinned": true,
      "isBlocked": false,
      "timestamp": "2025-11-06T00:00:00Z"
    },
    {
      "provider": "OpenAI",
      "stage": "visual",
      "modelId": "gpt-4o-mini",
      "source": "AutomaticFallback",
      "reasoning": "Using automatic fallback: Safe default",
      "fallbackReason": "Project-override 'gpt-4o' was unavailable",
      "isPinned": false,
      "isBlocked": false,
      "timestamp": "2025-11-06T00:00:05Z"
    }
  ],
  "totalCount": 2
}
```

---

**Architecture Version:** 1.0  
**Last Updated:** 2025-11-06  
**Status:** ✅ Production-Ready
